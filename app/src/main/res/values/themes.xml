<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Primary brand color: Background -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color: Button -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PlaylistMaker.Icons">
        <item name="colorPrimary">@color/gray</item>
    </style>

    <style name="Theme.PlaylistMaker.Switchers">
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryVariant">@color/gray</item>
    </style>

    <style name="Theme.PlaylistMaker.SearchEditTexts">
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/black_light</item>
        <item name="background">@drawable/rounded_edittext</item>
    </style>
</resources>
